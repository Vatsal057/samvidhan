# Pin the light "paper" theme the UI is designed around, regardless of the
# viewer's OS preference.
[theme]
base = "light"
primaryColor = "#1c4ed8"
backgroundColor = "#faf9f7"
secondaryBackgroundColor = "#f5f5f4"
textColor = "#1c1917"

[browser]
gatherUsageStats = false
