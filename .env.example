# Copy to .env and fill in. .env is gitignored — never commit real tokens.

# Required only for answer GENERATION (retrieval works without it).
# Get a free token: https://huggingface.co/settings/tokens
HF_TOKEN=

# --- Optional overrides (sane defaults live in src/samvidhan/config.py) ---
# GEN_MODEL=mistralai/Mistral-7B-Instruct-v0.3
# EMBED_MODEL=all-MiniLM-L6-v2
# TOP_K=5
# USE_RERANKER=false
# CHROMA_DIR=chroma_db
